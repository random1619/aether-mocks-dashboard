---
phase: 06-introduce-the-sectional-timing-for-different-types-of-mocks-
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - public/index.html
  - public/css/dashboard.css
  - public/js/dashboard.js
  - public/css/mock-main.css
  - public/js/mock-core.js
autonomous: true
requirements:
  - LOGIC-02
user_setup: []

must_haves:
  truths:
    - "Sectional timer toggle switch exists in the dashboard settings sidebar"
    - "Toggling the dashboard switch updates localStorage.sectionalTimerOverride to '10' or 'default'"
    - "Welcome screens for mock exams display the same toggle switch synchronized with localStorage"
    - "Starting a sectional mock quiz with the toggle ON correctly sets all section timers to 10 minutes"
  artifacts: []
  key_links: []
---

<objective>
Replace the sectional timer select dropdown with a clean, iOS-style toggle switch on both the main dashboard and mock welcome screens, forcing a 10-minute limit when active.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-introduce-the-sectional-timing-for-different-types-of-mocks-/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Toggle CSS Styles</name>
  <files>
    <file>public/css/dashboard.css</file>
    <file>public/css/mock-main.css</file>
  </files>
  <action>
    Append custom toggle switch styling (with `.switch`, `.slider`, `.slider.round`, etc.) to both dashboard.css and mock-main.css so that input checkboxes can be styled as toggles.
  </action>
  <verify>
    Verify files are valid CSS.
  </verify>
  <acceptance_criteria>
    - both css files contain the switch toggle CSS rules.
  </acceptance_criteria>
  <done>Toggle switch CSS styles injected into both stylesheet files</done>
</task>

<task type="auto">
  <name>Task 2: Refactor dashboard index.html to replace select dropdown with toggle</name>
  <files>
    <file>public/index.html</file>
  </files>
  <action>
    In index.html, replace the sectional-timer-select select element and its options with a label and a switch toggle:
    - `<input type="checkbox" id="sectional-timer-toggle">`
  </action>
  <verify>
    Ensure index.html contains the new toggle switch element.
  </verify>
  <acceptance_criteria>
    - index.html updated and contains #sectional-timer-toggle.
  </acceptance_criteria>
  <done>index.html refactored to use toggle switch</done>
</task>

<task type="auto">
  <name>Task 3: Refactor dashboard.js to bind toggle switch logic</name>
  <files>
    <file>public/js/dashboard.js</file>
  </files>
  <action>
    In dashboard.js:
    - Update `DOM.timerSelect` to `DOM.timerToggle = document.getElementById('sectional-timer-toggle')`.
    - In `bindEvents()`, check and set `timerToggle.checked` state from `localStorage.getItem('sectionalTimerOverride') === '10'`.
    - Handle `change` event on `timerToggle` to save `'10'` (checked) or `'default'` (unchecked) to localStorage.
  </action>
  <verify>
    Ensure dashboard.js binds changes to the checkbox toggle.
  </verify>
  <acceptance_criteria>
    - dashboard.js updated and correctly manages state of #sectional-timer-toggle.
  </acceptance_criteria>
  <done>dashboard.js bindings updated to handle toggle switch</done>
</task>

<task type="auto">
  <name>Task 4: Refactor mock-core.js welcome screen to display switch toggle</name>
  <files>
    <file>public/js/mock-core.js</file>
  </files>
  <action>
    In mock-core.js:
    - In `injectSettingsUI()`, replace the HTML select block with a checkbox switch toggle (`#sectionalTimerOverrideToggle`).
    - In the start button event listener, retrieve the checkbox toggle value (`checked` state) and store `'10'` or `'default'` in `localStorage` under `sectionalTimerOverride`.
  </action>
  <verify>
    Ensure mock-core.js is updated with the toggle switch logic.
  </verify>
  <acceptance_criteria>
    - mock-core.js uses the toggle switch instead of select dropdown.
  </acceptance_criteria>
  <done>mock-core.js welcome panel updated to use toggle switch</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Verify toggle switch renders correctly on Aether Mocks dashboard.
- Verify toggling dashboard switch updates localStorage correctly.
- Launch a mock and verify welcome panel toggle matches dashboard selection.
- Start quiz and verify first section is restricted to 10 minutes if toggle is enabled.
</verification>

<success_criteria>
- Toggle switches fully functional on both dashboard and mock quiz pages.
- Strict 10-minute timing limit applies correctly when toggle is checked.
</success_criteria>
