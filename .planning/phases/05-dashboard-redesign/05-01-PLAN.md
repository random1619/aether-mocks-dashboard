---
phase: 05-dashboard-redesign
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - public/index.html
  - public/css/dashboard.css
  - public/js/dashboard.js
autonomous: true
requirements:
  - LAYOUT-01
  - LAYOUT-02
  - LAYOUT-03
  - LOGIC-01
user_setup: []

must_haves:
  truths:
    - "Mock dashboard loads successfully in light mode without console errors"
    - "All search, sorting, and filter controls are styled professionally and work"
    - "Provider badges are color-coded with readable pastel themes"
    - "Completed status toggling updates counters and is persisted correctly"
  artifacts: []
  key_links: []
---

<objective>
Redesign the main entry dashboard into a premium, clean light-mode interface, separating inline styles/scripts into dedicated dashboard.css and dashboard.js assets, and refining provider color styles.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard-redesign/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Dashboard Stylesheet (dashboard.css)</name>
  <files>
    <file>public/css/dashboard.css</file>
  </files>
  <read_first>public/index.html</read_first>
  <action>
    Create a new stylesheet public/css/dashboard.css containing:
    - Clean light-mode theme variables (slate background, white cards, deep slate text, subtle borders).
    - Subtle low-opacity animated background orbs for premium visual depth.
    - Professional search bar styling with clear outline transition states.
    - Light-themed, accessible filter pills (Provider, Subject) with distinct border and hover actions.
    - Card container redesign with slate accents, rounded borders, clean margins, and micro-animations on hover.
    - Accordion layout styling for roadmap tracks, timelines, and progress bars matching the new color theme.
  </action>
  <verify>
    Ensure file compiles or is formatted cleanly as valid CSS.
  </verify>
  <acceptance_criteria>
    - public/css/dashboard.css exists and is syntactically valid CSS.
  </acceptance_criteria>
  <done>dashboard.css created and contains complete dashboard design system rules</done>
</task>

<task type="auto">
  <name>Task 2: Create Dashboard Script Asset (dashboard.js)</name>
  <files>
    <file>public/js/dashboard.js</file>
  </files>
  <read_first>public/index.html</read_first>
  <action>
    Create a new JavaScript script public/js/dashboard.js containing:
    - Application state management (filters, sort, search query, page limits).
    - Palette mapping configuration defining bg, text, border, and main accent colors for each vendor and subject.
    - DOM bindings and event listener attachments for filters, search bar, sort select, and view mode toggles.
    - Dynamic card template generation for grid list rendering.
    - Dynamic roadmap timeline generator for syllabus tracks and chapter explorer.
    - Stats counters update logic and completion persistence functions.
  </action>
  <verify>
    Ensure script is syntactically valid JavaScript.
  </verify>
  <acceptance_criteria>
    - public/js/dashboard.js exists and has no syntax errors.
  </acceptance_criteria>
  <done>dashboard.js created and includes full interactivity and rendering state logic</done>
</task>

<task type="auto">
  <name>Task 3: Redesign index.html structure</name>
  <files>
    <file>public/index.html</file>
  </files>
  <read_first>public/index.html</read_first>
  <action>
    Refactor public/index.html:
    - Strip the massive inline CSS block from `<style>` inside `<head>`.
    - Strip the inline controller script block from `<script>` before `</body>`.
    - Inject link stylesheet tags referencing css/dashboard.css.
    - Inject script defer tags referencing js/dashboard.js.
    - Ensure all main page DOM elements (brand header, stats wrappers, aside sidebar filter components, content grids, etc.) match structural selectors referenced by JS.
  </action>
  <verify>
    Ensure page HTML syntax is correct.
  </verify>
  <acceptance_criteria>
    - index.html is updated, contains no inline style blocks or internal script blocks, and successfully references external assets.
  </acceptance_criteria>
  <done>index.html refactored to modular architecture and clean structure</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Verify mock dashboard visual layout in browser.
- [ ] Confirm filters and search correctly update mock cards list.
- [ ] Confirm roadmap view and topic accordion operates smoothly.
</verification>

<success_criteria>
- All tasks completed.
- Dashboard visual design is in premium clean light-mode layout.
- All filters, view toggles, timelines, and status counters function correctly without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-redesign/05-01-SUMMARY.md`
</output>
