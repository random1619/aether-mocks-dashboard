---
phase: 02-page-unification-cleaning
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - public/Pundiits/**/*.html
  - public/static GK/**/*.html
autonomous: true
requirements:
  - LAYOUT-02
  - LAYOUT-03
user_setup: []

must_haves:
  truths:
    - "Mock HTML files have no inline style attributes (style='...')"
    - "Mock HTML files contain standard layout structure elements: .question-card, .quiz-option, .options-list"
  artifacts: []
  key_links: []
---

<objective>
Clean the mock test pages by removing deprecated inline style attributes and injecting the new layout structure classes.

Purpose: Uniform layout structure across various vendor pages.
Output: Cleaned HTML templates ready to link centralized assets.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-page-unification-cleaning/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize Layout Structure and Strip Styles</name>
  <files>public/Pundiits/**/*.html, public/static GK/**/*.html</files>
  <read_first>.planning/phases/02-page-unification-cleaning/02-CONTEXT.md</read_first>
  <action>
    Parse all mock HTML files. For each file:
    - Remove inline style="..." declarations from divs, tables, spans, and paragraphs.
    - Inject standard layout classes:
      - Assign class="question-card" to each main question box div.
      - Assign class="options-list" to choice wrapper elements.
      - Assign class="quiz-option" to individual answer options.
    - This can be done using a Python script (e.g. updating or running a clean script in public/ or root) to process files programmatically.
  </action>
  <verify>
    Verify that processed HTML files no longer contain custom inline CSS background/margin declarations, and have class="question-card" and class="quiz-option" injected.
  </verify>
  <acceptance_criteria>
    - Mock HTML files do not contain inline background/color definitions
    - Mock HTML files contain class="question-card"
  </acceptance_criteria>
  <done>Mock pages have clean layout structures and no inline styles</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Sample checks on a few processed HTML files.
- [ ] Confirm no inline styles are left.
</verification>

<success_criteria>
- All tasks completed
- HTML structures are standardized
</success_criteria>

<output>
After completion, create `.planning/phases/02-page-unification-cleaning/02-01-SUMMARY.md`
</output>
