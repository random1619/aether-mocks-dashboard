---
phase: 02-page-unification-cleaning
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - public/Pundiits/**/*.html
  - public/static GK/**/*.html
autonomous: true
requirements:
  - INTEGRATE-01
  - INTEGRATE-02
user_setup: []

must_haves:
  truths:
    - "Mock HTML files link to public/css/mock-main.css via depth-aware relative path"
    - "Mock HTML files link to public/js/mock-core.js via depth-aware relative path"
    - "MathJax/KaTeX math formula script tags are preserved and load successfully"
  artifacts: []
  key_links: []
---

<objective>
Link the centralized styling and interactivity resources to all mock pages and clean up old vendor scripts while whitelisting math formula loaders.

Purpose: Connect restructured pages to unified client-side logic and styles.
Output: Integrated and interactive mock portals.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-page-unification-cleaning/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject Central Assets and Strip Scripts</name>
  <files>public/Pundiits/**/*.html, public/static GK/**/*.html</files>
  <read_first>.planning/phases/02-page-unification-cleaning/02-CONTEXT.md</read_first>
  <action>
    Iterate over all mock HTML files. For each file:
    - Determine its folder nesting depth relative to the public/ root (e.g., public/Pundiits/75/Math/... is depth 4).
    - Insert a link tag in the head for mock-main.css and a script tag at the end of the body for mock-core.js with correct relative paths.
    - Remove all old vendor JS file links and inline analytics trackers (such as Google Analytics or other metrics script tags).
    - Ensure script tags containing mathjax, MathJax, or katex are NOT removed.
    - This can be performed programmatically via a Python conversion script to ensure speed and accuracy.
  </action>
  <verify>
    Check that mock pages contain relative links to mock-main.css and mock-core.js and do not reference old vendor trackers.
  </verify>
  <acceptance_criteria>
    - Mock HTML files contain "mock-main.css" relative link
    - Mock HTML files contain "mock-core.js" relative script
    - MathJax/KaTeX script references are present in files that need them
  </acceptance_criteria>
  <done>Mock pages link to central assets and have clean scripts</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Sample check file heads and bodies.
- [ ] Verify that LaTeX script tags are intact.
</verification>

<success_criteria>
- All tasks completed
- Central assets linked with correct depth relative paths
- Bloated vendor scripts stripped
</success_criteria>

<output>
After completion, create `.planning/phases/02-page-unification-cleaning/02-02-SUMMARY.md`
</output>
