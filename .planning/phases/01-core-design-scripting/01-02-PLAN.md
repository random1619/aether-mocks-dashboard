---
phase: 01-core-design-scripting
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - public/js/mock-core.js
autonomous: true
requirements:
  - LOGIC-01
  - LOGIC-02
  - LOGIC-03
  - LOGIC-04
user_setup: []

must_haves:
  truths:
    - "public/js/mock-core.js implements a visual count-down timer with expiry actions"
    - "public/js/mock-core.js implements next/previous and jump-to-question navigation"
    - "public/js/mock-core.js tracks selected options for each question index in-memory"
    - "public/js/mock-core.js contains whitelisting logic for MathJax formula rendering engines"
  artifacts:
    - public/js/mock-core.js
  key_links: []
---

<objective>
Develop the centralized Javascript quiz controller containing navigation, option handling, timers, and LaTeX script whitelists.

Purpose: Replace fragmented and heavy vendor tracking/interactivity scripts with a lightweight client-side controller.
Output: public/js/mock-core.js containing the interactivity logic.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-design-scripting/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Centralized Mock script</name>
  <files>public/js/mock-core.js</files>
  <read_first>.planning/phases/01-core-design-scripting/01-CONTEXT.md</read_first>
  <action>
    Create a new Javascript file at public/js/mock-core.js. Define the core controller classes/functions:
    - MockState: Tracks activeState (current question index, array of selected option indexes, time remaining).
    - initTimer(durationSeconds, displayElementId): Set up countdown timer that visually writes MM:SS to displayElementId. Alert when time is 0.
    - navigateToQuestion(index): Hides all question container nodes except the one corresponding to the selected index. Updates active question styling in the navigation sidebar.
    - selectOption(questionIndex, optionIndex): Handles click events on options, sets selected class state, and records user answer.
    - preserveLaTeX(): Ensure any scripts loading mathJax/KaTeX (matching patterns like 'mathjax' or 'katex') are whitelisted and execute successfully.
  </action>
  <verify>
    Check that the file exists and compiles without errors.
  </verify>
  <acceptance_criteria>
    - public/js/mock-core.js contains "initTimer"
    - public/js/mock-core.js contains "navigateToQuestion"
    - public/js/mock-core.js contains "selectOption"
  </acceptance_criteria>
  <done>public/js/mock-core.js exists and implements interactivity logic</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Confirm public/js/mock-core.js exists and is non-empty.
- [ ] Verify that logic functions are declared correctly.
</verification>

<success_criteria>
- All tasks completed
- public/js/mock-core.js exists and compiles
- Functions cover navigation, option state, and timers
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-design-scripting/01-02-SUMMARY.md`
</output>
