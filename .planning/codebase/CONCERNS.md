# Concerns Map
## Current Concerns
- Managing CSS conflicts in standalone provider HTMLs.
- Syncing `mocks-data.js` requires a Python execution prior to deployment.
- High number of provider HTML files makes global changes require recursive patching.
